apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
spec:
  type: {{ .Values.db-service.type }}
  ports:
    - port: {{ .Values.db-service.port }}
      targetPort: 5432
      protocol: TCP
      name: http
  selector:
    name: db
