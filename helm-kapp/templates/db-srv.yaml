apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-kapp.fullname" . }}
  labels:
    {{- include "helm-kapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-kapp.selectorLabels" . | nindent 4 }}
